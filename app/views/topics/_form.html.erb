<%= form_for(topic) do |f| %>
  <% if topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title：收拾他日你个description：text %>
    <%= f.text_field :title：收拾他日你个description：text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
